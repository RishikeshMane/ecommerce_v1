import { AddressList } from "./address-list.model";

export class TestDetail
{
    country: string='';
    state: string='';
    city: string='';
    pincode: string='';
    address: string='';
    isDeliveryAddress: number=0;
    flagCode: string='';
}